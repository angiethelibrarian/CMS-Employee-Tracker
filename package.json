{
  "name": "cms-employee-tracker",
  "version": "1.0.0",
  "description": "Employee Tracker assignment",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node dist/index.js"
  },
  "author": "Angelica Mora",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^12.0.1",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
